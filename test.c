#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char buffer[] = "Use your Google Account to sign in";
    char *token = NULL;
    int size;
    switch (buffer[3])
    if (strncmp(buffer, "Use ", size = strlen("Use ")) == 0){
        token = buffer + size;
    }
    printf("%s\n", token);
}
    


